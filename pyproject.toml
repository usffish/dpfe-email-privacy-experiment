[project]
name = "dpfe-email-privacy-experiment"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "peft",
    "bitsandbytes",
    "opacus",
    "tabulate",
    "numpy",
]
